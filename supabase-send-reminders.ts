// RETIRED compatibility pointer.
// The only deployable reminder implementation is:
//   supabase/functions/send-reminders/index.ts
// Keeping one canonical copy prevents a manual dashboard deploy from silently
// dropping newer privacy, nudge, and delivery safeguards.
